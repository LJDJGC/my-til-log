## 2026-04-21 学習実績

### 📚 完了タスク
- [x] ポートフォリオ「Routine Tracker」の技術スタック選定（Next.js, TypeScript, Tailwind CSS）
- [x] `create-next-app` によるプロジェクトの初期化と環境構築
- [x] 開発サーバーの起動（`npm run dev`）とブラウザ表示確認
- [x] 初期ファイルのクリーンアップ（`page.tsx`, `globals.css` の整理）
- [x] 開発環境の警告（複数lockfileによるルートディレクトリ誤認）の解消

### 💡 学びのポイント
**新規習得：**
- **Next.js App Router**: フォルダ構成がそのままURL（ルーティング）になる最新の仕組み。
- **npxコマンド**: ライブラリをインストールせずに一時実行してプロジェクトを作成する手法。
- **Tailwind CSSの基本**: `text-3xl` や `font-bold` など、クラス名を使って直感的にデザインを当てる基礎。

**理解深化：**
- **開発環境の整合性**: プロジェクトフォルダの外（ホームディレクトリなど）に不要な設定ファイル（`package-lock.json`など）があると、ツールが混乱して予期せぬ挙動をすることを学んだ。
- **クリーンアップの重要性**: 自動生成されたコードを一度リセットすることで、自分が書いたコードがどこに反映されているかを明確に把握できる。

**トラブルシューティング：**
- **Next.jsの警告対応**: 「Multiple lockfiles detected」という警告に対し、`/home/ljdjgc/` 直下の不要なファイルを削除することで、Next.jsが正しくプロジェクトのルートを認識できる状態に修正した。

### 🔗 実装コード
- リポジトリ：未作成
- 主要コード：
```tsx
// src/app/page.tsx
// 最小限の構成に整理し、Tailwind CSSでスタイリングを適用
export default function Home() {
  return (
    <main className="p-10">
      <h1 className="text-3xl font-bold text-blue-600">
        Routine Tracker：学習 × 筋トレ
      </h1>
      <p className="mt-4 text-gray-600">
        今日からここに、あなたの努力を刻んでいきます。
      </p>
    </main>
  );
}
```

### ⏭️ Next Action
- [ ] 昨日設計した「Session型」をプロジェクト内に定義ファイルとして配置する（所要時間目安：20分）
- [ ] ダミーデータを作成し、`map`関数を使って画面に「記録一覧」を表示させる（所要時間目安：60分）





## Learning Results - April 21, 2026

### 📚 Completed Tasks
- [x] Selection of the technology stack for the portfolio "Routine Tracker" (Next.js, TypeScript, Tailwind CSS)
- [x] Project initialization and environment setup using `create-next-app`
- [x] Starting the development server (`npm run dev`) and checking browser display
- [x] Cleaning up initial files (organizing `page.tsx` and `globals.css`)
- [x] Resolving development environment warnings (root directory misidentification due to multiple lock files)

### 💡 Key Learning Points
**Newly Acquired:**
- **Next.js App Router**: The latest mechanism where the folder structure directly becomes the URL (routing).
- **npx command**: A method for creating a project by temporarily executing it without installing libraries.

- **Tailwind CSS Basics**: The fundamentals of intuitively applying design using class names such as `text-3xl` and `font-bold`.

**Deepening Understanding:**
- **Development Environment Consistency**: Learned that having unnecessary configuration files (such as `package-lock.json`) outside the project folder (e.g., home directory) can confuse tools and lead to unexpected behavior.
- **The Importance of Cleanup**: Resetting automatically generated code once provides a clear understanding of where your own code is reflected.

**Troubleshooting:**
- **Next.js Warning Resolution**: Addressed the "Multiple lockfiles detected" warning by deleting unnecessary files directly under `/home/ljdjgc/`, allowing Next.js to correctly recognize the project root.

### 🔗 Implementation Code
- Repository: Not yet created
- Main Code:
```tsx
// src/app/page.tsx
// Organized into a minimal configuration and styled with Tailwind CSS
export default function Home() {
return (
<main className="p-10">
<h1 className="text-3xl font-bold text-blue-600">
Routine Tracker: Learning × Strength Training
</h1>
<p className="mt-4 text-gray-600">
From today, your efforts will be recorded here.
</p>
</main>
);
}
```
### ⏭️ Next Action
- [ ] Place the "Session type" designed yesterday as a definition file in the project (estimated time: 20 minutes)
- [ ] Create dummy data and use the `map` function to display a "record list" on the screen (estimated time: 60 minutes).