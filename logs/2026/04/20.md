## 2026-04-20 学習実績

### 📚 完了タスク
- [x] ポートフォリオ案「学習 × 筋トレ 統合トラッカー」のテーマおよび全体設計の決定
- [x] Step1：TypeScriptによるデータ構造設計（コア機能・差別化機能の型定義）
- [x] AIの1行解説を用いたコードの徹底理解と、何も見ずに書けるまでの反復練習

### 💡 学びのポイント
**新規習得：**
- TypeScriptの基本構文（`type`による独自型の宣言、`interface`によるオブジェクト定義）
- ユニオン型（例：`"study" | "workout"`）を用いて、特定の文字列しか許可しない堅牢なルールを作る方法
- `?`を用いたオプショナルプロパティ（あってもなくても良い項目）の定義方法

**理解深化：**
- アプリ開発において「機能から作り始める」のではなく、「まずは型設計（データ構造）から入る」ことの重要性。型でドメイン（ルール）を表現することが、バグを防ぎ、面接等でも評価される設計に繋がることを理解した。
- 動画で推奨されていた「コードを1行ずつ解説させ、何も見ずに自力で書けるまで反復する」という学習法を実践し、言語化して理解するプロセスが知識の定着に非常に効果的であることを実感した。

**トラブルシューティング：**
- 初めて見るTypeScriptの構文（`export`の意味や、型の指定方法など）に戸惑ったが、AIに「小学生でもわかるように1行ずつ解説して」と依頼することで、記号一つ一つの意味を明確に理解し、暗記・記述できるレベルまで落とし込むことができた。

### 🔗 実装コード
- リポジトリ：未作成
- 主要コード：
```typescript
// 今回設計した主要なデータ構造（ドメイン表現）
export type SessionType = "study" | "workout";

export interface Session {
  id: string;
  type: SessionType;
  duration: number;
  date: string;
  note?: string; // メモは任意
}

// 差別化機能（バランス分析）のための型
export interface BalanceAnalysis {
  studyPercentage: number;
  workoutPercentage: number;
  isBalanced: boolean;
}
```

### ⏭️ Next Action
-[ ] Step2：記録機能（CRUD）に必要な状態管理（Zustandなど）やダミーデータのコードをAIに出力させ、1行ずつ解説してもらう（所要時間目安：30分）
- [ ] 解説を読み込み、CRUD機能の基盤となるコードを何も見ずに書けるまで反復練習する（所要時間目安：60分）





## Learning Results - April 20, 2026

### 📚 Completed Tasks
- [x] Deciding on the theme and overall design of the portfolio proposal "Learning x Strength Training Integrated Tracker"
- [x] Step 1: Data structure design using TypeScript (Type definition of core and differentiating functions)
- [x] Thorough understanding of code using AI's one-line explanation and repeated practice until able to write it without looking at anything

### 💡 Learning Points
**Newly Acquired:**
- Basic syntax of TypeScript (declaration of custom types using `type`, object definition using `interface`)
- How to create robust rules that only allow specific strings using union types (e.g., `"study" | "workout"`)
- How to define optional properties (items that may or may not be present) using `?`

**Deepened Understanding:**
- The importance of "starting with type design (data structure)" rather than "starting with functionality" in app development. I understood that expressing the domain (rules) with types prevents bugs and leads to designs that are well-received in interviews, etc.
- I practiced the learning method recommended in the video: "Explain the code line by line and repeat until I can write it on my own without looking at anything." I realized that the process of verbalizing and understanding is extremely effective for knowledge retention.

**Troubleshooting:**
- I was confused by the unfamiliar TypeScript syntax (the meaning of `export`, how to specify types, etc.), but by asking the AI ​​to "explain it line by line in a way that even an elementary school student could understand," I was able to clearly understand the meaning of each symbol and internalize it to a level where I could memorize and write it.

### 🔗 Implementation Code
- Repository: Not yet created
- Main Code:
```typescript
// Main data structure designed this time (domain representation)
export type SessionType = "study" | "workout";
export interface Session {
id: string;
type: SessionType;
duration: number;
date: string;
note?: string; // Note is optional
}
// Type for differentiation function (balance analysis)
export interface BalanceAnalysis {
studyPercentage: number;
workoutPercentage: number;
isBalanced: boolean;
}
```
### ⏭️ Next Action
-[ ] Step 2: Have the AI ​​output the code for state management (Zustand, etc.) and dummy data necessary for the recording function (CRUD), and have it explain each line (estimated time: 30 minutes)
- [ ] Read the explanation thoroughly and practice repeatedly until you can write the underlying code for CRUD functionality without looking at any references (estimated time: 60 minutes).
