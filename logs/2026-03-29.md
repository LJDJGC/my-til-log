## 2026-03-29 学習実績

### 📚 完了タスク
- [x] Next.js 開発環境のセットアップ（Ubuntu環境下にて `npx create-next-app` を実行）
- [x] プロジェクトのディレクトリ整理とリネーム（`my-app` → `dev/study-react`）
- [x] しまぶーのIT大学「Next.jsと一緒に学ぶReact講座」視聴（概念理解）
- [x] React公式チュートリアル「三目並べ」ハンズオン（序盤〜中盤）

### 💡 学びのポイント
**新規習得：**
- **`npx` コマンド:** パッケージをローカルにインストールせずに実行する仕組み。`create-next-app` 等の最新版を常に使う際に有効。
- **`npm run dev` と `build` の違い:** 開発中はホットリロードが効く `dev`、本番用は最適化と圧縮を行う `build` を使うという実務フローの理解。
- **Next.jsのフォルダ構造:** `src/app` (App Router) や `package.json` の役割を、実際のプロジェクトを触りながら把握。

**理解深化：**
- **`package.json` の重要性:** 単なるメモ書きではなく、環境を再現するための「設計図」としての価値。
- **TypeScript/Tailwind CSSの自動導入:** モダン開発に必要なツールがセットアップ時に一括で設定される恩恵を体験。

**トラブルシューティング：**
- **事象:** `yarn` 未インストールによるエラーと、Ubuntuからの `cmdtest` インストール勧告。
- **解決:** メンター（AI）の助言により、別のツールである `cmdtest` を避け、既存の `npm` 環境を活かせる `npx` で代替。
- **事象:** プロジェクトの作成場所ミス。
- **解決:** `mv` コマンドを用いてディレクトリを `~/dev` 下へ適切に移動し、`package.json` 内の名前も修正して整合性を確保。

### 🔗 実装コード
- リポジトリ：未作成（ローカル：`study-react`）
- 主要コード：
```javascript
// src/app/page.tsx (初期表示の確認)
export default function Home() {
  return (
    <main>
      <h1>React Study Start</h1>
      {/* 修正・保存した瞬間にブラウザが変わるホットリロードを体験 */}
    </main>
  );
}
```

### ⏭️ Next Action
- [ ] React公式チュートリアル：三目並べの完走（「Stateの引き上げ」の理解）（所要時間目安：90分）
- [ ] 完了した三目並べのコードを `study-react` プロジェクト内に反映させる（所要時間目安：30分）







## Learning Record - March 29, 2026

### 📚 Completed Tasks
- [x] Setup of Next.js development environment (running `npx create-next-app` in an Ubuntu environment)
- [x] Organizing and renaming project directories (`my-app` → `dev/study-react`)
- [x] Watching Shimabu's IT University "React Course with Next.js" (Understanding concepts)
- [x] React official tutorial "Tic-Tac-Toe" hands-on (beginner to mid-stages)

### 💡 Learning Points
**Newly Acquired:**
- **`npx` command:** A mechanism to run packages without installing them locally. Useful when always using the latest version of `create-next-app`, etc.

- **Difference between `npm run dev` and `build`:** Understanding the practical workflow of using `dev` for hot reloading during development and `build` for optimization and compression in production.

- **Next.js folder structure:** Understanding the roles of `src/app` (App Router) and `package.json` by working with an actual project.

**Deepening understanding:**
- **The importance of `package.json`:** Its value as a "blueprint" for reproducing the environment, rather than just a memo.

- **Automatic deployment of TypeScript/Tailwind CSS:** Experiencing the benefits of having the tools necessary for modern development configured all at once during setup.

**Troubleshooting:**
- **Issue:** Error due to `yarn` not being installed, and a recommendation to install `cmdtest` from Ubuntu.

- **Solution:** Based on advice from the mentor (AI), avoided using another tool, `cmdtest`, and substituted it with `npx`, which leverages the existing `npm` environment.

- **Issue:** Incorrect project creation location.

- **Solution:** Used the `mv` command to move the directory correctly under `~/dev` and corrected the name in `package.json` to ensure consistency.


### 🔗 Implementation Code
- Repository: Not yet created (Local: `study-react`)
- Main Code:
```javascript
// src/app/page.tsx (Initial display check)
export default function Home() {
return (
<main>
<h1>React Study Start</h1>
{/* Experience hot reload where the browser changes the moment you modify and save */}
</main>
);
}
```
### ⏭️ Next Action
- [ ] React Official Tutorial: Completing Tic-Tac-Toe (Understanding "State Pulling") (Estimated time: 90 minutes)
- [ ] Reflecting the completed Tic-Tac-Toe code into the `study-react` project (Estimated time: 30 minutes)