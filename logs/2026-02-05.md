## 2026-02-05 学習実績

### 📚 完了タスク
- [x] The Odin Project: Project: Library (Assignment 5)
- [x] 書籍削除機能のロジック設計（疑似コード作成）
- [x] `crypto.randomUUID()` による書籍への一意なID付与の実装

### 💡 学びのポイント
**新規習得：**
- **HTMLデータ属性 (`dataset`)**: JSのデータをHTML要素に「目印」として安全に埋め込む手法。
- **`crypto.randomUUID()`**: 重複の可能性が極めて低いユニークIDを生成するモダンな方法。

**理解深化：**
- **データの不変性**: 配列のインデックス（0, 1, 2...）をIDに使うと、削除時に番号がズレる「負債」を生むが、UUIDならその心配がないこと。
- **Single Source of Truth（信頼できる唯一の情報源）**: 配列（データ）を正しく操作すれば、それを元に画面（表示）を再描画するだけで整合性が保てるという考え方。

**トラブルシューティング：**
- 「削除ボタンを押した時に、どの本を消せばいいか分からなくなる」という懸念に対し、HTMLにIDを刻印（dataset）し、それをキーに配列を `filter` するという解決策を見出した。

### 🔗 実装コード
- リポジトリ：未作成
- 主要コード：
```javascript
function Book(title, author, pages, isRead) {
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.isRead = isRead;
  // インデックスのズレ問題を解決する「背番号」
  this.id = crypto.randomUUID();
}

// 今後の実装予定部分
// card.dataset.id = book.id; // HTMLにIDを刻印
// myLibrary = myLibrary.filter(b => b.id !== targetId); // 配列から除去
```

### ⏭️ Next Action
- [ ] `displayBook` 内での削除ボタン作成と `dataset` の付与（所要時間目安：30分）
- [ ] 削除ボタンのクリックイベントと `filter` を使った削除処理の実装（所要時間目安：45分）






## 2026-02-05 Learning Achievements

### 📚 Completed Tasks
- [x] The Odin Project: Project: Library (Assignment 5)
- [x] Logic Design for Book Deletion Function (Writing Pseudocode)
- [x] Implementing Unique ID Assignment for Books Using `crypto.randomUUID()`

### 💡 Key Learning Points
**New Learnings:**
- **HTML Data Attributes (`dataset`)**: A method for safely embedding JS data in HTML elements as "markers."
- **`crypto.randomUUID()`**: A modern method for generating unique IDs with an extremely low probability of duplication.

**Deeper Understanding:**
- **Data Immutability**: Using array indexes (0, 1, 2, etc.) as IDs creates the "debt" of mismatching numbers upon deletion, but this is not a concern with UUIDs.
- **Single Source of Truth**: The idea is that if the array (data) is manipulated correctly, consistency can be maintained simply by redrawing the screen (display) based on that data.

**Troubleshooting:**
- To address the concern that "when pressing the delete button, it becomes difficult to know which book to delete," we found a solution by adding an ID (dataset) to the HTML and using that as a key to `filter` the array.

### 🔗 Implementation Code
- Repository: Not yet created
- Main Code:
```javascript
function Book(title, author, pages, isRead) {
this.title = title;
this.author = author;
this.pages = pages;
this.isRead = isRead;
// "Uniform number" to solve the index misalignment problem
this.id = crypto.randomUUID();
}

// Future Implementation
// card.dataset.id = book.id; // Add ID to HTML
// myLibrary = myLibrary.filter(b => b.id !== targetId); // Remove from array
```

### ⏭️ Next Action
- [ ] Create a delete button in `displayBook` and assign `dataset` (Estimated time: 30 minutes)
- [ ] Delete button click event and `filter` Implementing a deletion process using (Estimated time required: 45 minutes)






## 2024-05-24 学習実績

### 📚 完了タスク
- [x] The Odin Project: Project Library（削除ボタンの実装）
- [x] `crypto.randomUUID()` を用いた一意のID付与
- [x] `dataset` を活用したDOM要素とデータの紐付け
- [x] `filter` メソッドによる配列からの特定の要素削除

### 💡 学びのポイント
**新規習得：**
- **dataset属性**: HTML要素に `data-` から始まる独自の属性を持たせ、JSから `element.dataset` で簡単に取得・操作できる。
- **crypto.randomUUID()**: 重複の可能性が極めて低い一意識別子を生成する方法。

**理解深化：**
- **IDによるデータ管理**: 配列のインデックス（0, 1, 2...）ではなく不変のIDを使うことで、削除や並び替えをしてもデータがズレない堅牢な設計になる。
- **filterメソッド**: 条件に一致しないものだけを抽出して新しい配列を作ることで、「実質的な削除」を実現する考え方。

**トラブルシューティング：**
- **IDの紐付け**: 「どの削除ボタンがどのデータに対応するか」という課題に対し、生成時に `card.dataset.id = book.id` と書き込むことで解決。

### 🔗 実装コード
- リポジトリ：未作成
- 主要コード：
```javascript
// 表示時にIDを付与
card.dataset.id = book.id;

// 削除処理のコア
function removeBook(id) {
  myLibrary = myLibrary.filter(book => book.id !== id);
  displayBook();
}
```

### ⏭️ Next Action
- [ ] 各書籍カードに「既読/未既読」を切り替えるトグルボタンを追加する（所要時間目安：60分）
