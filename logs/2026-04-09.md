## 2026-04-09 学習実績

### 📚 完了タスク
- [x] React公式チュートリアル（Tic Tac Toe）：タイムトラベル機能（`jumpTo`）の実装完了およびチュートリアル全編の完走

### 💡 学びのポイント
**新規習得：**
- **Reactの全体像の把握**：コンポーネントの分割、Propsによるデータの受け渡し、Stateの引き上げ（Lifting State Up）、派生状態（Derived State）、イミュータビリティ（非破壊的更新）といった、React開発の根幹となる設計思想を一通り実践した。
- **Just-in-Time学習の精神**：「100%理解してから進む（Just-in-Case）」のではなく、「60%の理解で手を動かし、必要になったら辞書として戻ってくる（Just-in-Time）」という、実務ベースの学習サイクル・メンタルモデルを獲得した。

**理解深化：**
- **UIはStateの関数である**：過去の履歴（State）を `currentMove` で切り替えるだけで、盤面のUIが自動的に過去の状態に再描画される（タイムトラベル）という、Reactの宣言的UIの強力さを体感した。

**トラブルシューティング：**
- **課題**：概念（特に履歴の配列操作やStateの引き上げ）が複雑で、完全に腹落ちしていない感覚がある。
- **解決（マインドセット）**：今のフェーズでは「完璧な理解」よりも「全体像を掴んで次に進む」ことを優先。今後のオリジナルアプリ（Weather App）開発を通じて、実践の中で理解の穴を埋めていく戦略へシフトした。

### 🔗 実装コード
- リポジトリ：未作成（ローカル環境）
- 主要コード：
```javascript
// タイムトラベルを制御するGameコンポーネントのコアロジック
// history（過去の盤面配列）とcurrentMove（現在見ている手番）を分離して管理
function jumpTo(nextMove) {
  setCurrentMove(nextMove);
  // xIsNextはStateとして持たず、nextMove(手番)が偶数かどうかで算出する（Derived State）
}

// 履歴からボタンのリストを生成する処理（一意のkeyを設定）
const moves = history.map((squares, move) => {
  let description = move > 0 ? 'Go to move #' + move : 'Go to game start';
  return (
    <li key={move}>
      <button onClick={() => jumpTo(move)}>{description}</button>
    </li>
  );
});
```

### ⏭️ Next Action
- [ ] TypeScriptの基礎学習開始（動画教材等でのインプット）（所要時間目安：60分）
- [ ] 次のプロジェクト「Weather App」をReact + TypeScriptで構築するための環境準備（Vite + TS）（所要時間目安：30分）






## Learning Results - April 9, 2026

### 📚 Completed Tasks
- [x] React Official Tutorial (Tic Tac Toe): Completed implementation of the time travel function (`jumpTo`) and completed the entire tutorial.

### 💡 Key Learning Points
**New Acquisition:**
- **Understanding the Overall Picture of React**: Practiced the core design principles of React development, including component splitting, data passing using Props, State Lifting, Derived State, and immutability (non-destructive updates).
- **The Spirit of Just-in-Time Learning**: Acquired a practical learning cycle and mental model of "getting hands-on with 60% understanding and referring back to it as a reference when needed (Just-in-Time)," rather than "moving forward only after 100% understanding (Just-in-Case)."

**Deepening Understanding:**
- **UI is a function of State:** I experienced the power of React's declarative UI, where simply switching past history (State) with `currentMove` automatically redraws the UI on the board to its past state (time travel).

**Troubleshooting:**
- **Challenge:** The concepts (especially array manipulation of history and State retrieval) are complex, and I feel I haven't fully grasped them.
- **Solution (Mindset):** In this phase, prioritizing "grasping the big picture and moving forward" over "perfect understanding" has been shifted to a strategy of filling in gaps in understanding through practical application during future original app (Weather App) development.

### 🔗 Implementation Code
- Repository: Not yet created (local environment)
- Main Code:
```javascript
// Core logic of the Game component that controls time travel
// Separates and manages history (array of past board positions) and currentMove (current turn)
function jumpTo(nextMove) {
setCurrentMove(nextMove);
// xIsNext is not held as a State, but calculated based on whether nextMove (turn) is even (Derived State)
}
// Process to generate a list of buttons from history (set a unique key)
const moves = history.map((squares, move) => {
let description = move > 0 ? 'Go to move #' + move : 'Go to game start';
return (
<li key={move}>
<button onClick={() => jumpTo(move)}>{description}</button>
</li>
);
});
```
### ⏭️ Next Action
- [ ] Start learning the basics of TypeScript (input using video materials, etc.) (Estimated time: 60 minutes)
- [ ] Prepare the environment for building the next project "Weather App" with React + TypeScript (Vite + TS) (Estimated time: 30 minutes)