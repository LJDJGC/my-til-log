## 2026-02-08 学習実績

### 📚 完了タスク
- [x] The Odin Project: Factory Functions and the Module Pattern
- [x] Wes Bos: Scope (Global, Function, Block) の理解
- [x] プレイグラウンドでのコード実験

### 💡 学びのポイント
**新規習得：**
- **Factory Functions:** `new` を使わずにオブジェクトを生成する手法。`this` の挙動に悩まされるリスクが減り、直感的にオブジェクトを返せる。
- **Closure (クロージャ):** 関数が定義された時のスコープを「覚えている」仕組み。これを利用して、外部から書き換えられない「プライベート変数」を実装できる。
- **Module Pattern:** IIFE（即時実行関数）を使用して、アプリケーション全体で1つだけの「シングルトン」なオブジェクト（設定、電卓機能など）を作る手法。

**理解深化：**
- **Scopeの階層:** Global Scope（どこでも）、Function Scope（関数内）、Block Scope（`{}`内、`let/const`のみ）。Wes Bosの解説を通じ、`var` がなぜ現代のJSで非推奨なのかを再認識。
- **継承 vs 構成:** Factory Functionでは `Object.assign` を使い、必要な機能だけを「組み合わせて」新しいオブジェクトを作る。

**トラブルシューティング：**
- プライベート変数にアクセスしようとして `undefined` が返ってきた際、それが「バグ」ではなく、意図した「カプセル化」であることをコードレベルで確認できた。

### 🔗 実装コード
- リポジトリ：未作成（プレイグラウンドにて実行）
- 主要コード：
```javascript
// Factory Function with Private Variable (Closure)
const Player = (name, level) => {
  let health = 100; // プライベート変数（外部から変更不可）
  const die = () => console.log(`${name} is dead.`);
  const damage = (amount) => {
    health -= amount;
    if (health <= 0) die();
  };
  return { name, level, damage }; // healthやdieは露出させない
};

const user = Player('Sharp_Dev', 2);
user.damage(100); // 動作する
console.log(user.health); // undefined (安全)
```

### ⏭️ Next Action
- [ ] TOP: Factory Functions の Exercise/Project（Libraryアプリのリファクタリング等）に着手（所要時間目安：90分）

