## 2026-02-16 学習実績

### 📚 完了タスク
- [x] 三目並べ（Tic-Tac-Toe）プロジェクトの全体設計と役割分担の検討
- [x] カプセル化（IIFEとFactory関数）の概念理解
- [x] プロジェクト全体の疑似コード作成（設計段階）

### 💡 学びのポイント
**新規習得：**
- **IIFE（即時実行関数）とモジュールパターン：** `(function() { ... })();` という書き方で、定義と同時に実行し、内部変数を外部から隠蔽（カプセル化）する方法を学んだ。
- **シングルトン（1つだけのインスタンス）：** ゲームボードや審判役のように、アプリ内で1つしか存在してはいけないオブジェクトにはIIFEが適していることを知った。

**理解深化：**
- **Factory関数とIIFEの使い分け：** プレイヤーのように複数作るものは「Factory関数」、1つだけで良いものは「IIFE」という明確な基準を理解した。
- **Building a house from the inside out：** UI（見た目）を先に作るのではなく、まずコンソール上で動く「ロジック（内部）」から作り始める設計思考を学んだ。

**トラブルシューティング：**
- **「なぜ普通の関数ではなくIIFEなのか？」の解消：** 外部から勝手にデータを書き換えられないように保護するため、また誤って2つ目を作ってバグを生ませないための制約としてIIFEが必要であることを整理できた。

### 🔗 実装コード
- リポジトリ：https://github.com/LJDJGC/tictactoe-project-top
- 主要コード（構想案）：
```javascript
// Gameboard: 1つだけで良いのでIIFE（モジュール）
const gameboard = (function() {
  let board = ["", "", "", "", "", "", "", "", ""]; // プライベート変数
  const getBoard = () => board;
  return { getBoard };
})();

// Player: 2人分必要なのでFactory関数
const Player = (name, marker) => {
  return { name, marker };
};
```

### ⏭️ Next Action
- [ ] `Gameboard` オブジェクトの実装（配列の取得、値の更新、リセット機能）（所要時間目安：45分）
- [ ] `Player` ファクトリーの実装とインスタンス作成（所要時間目安：15分）
- [ ] `GameController` の骨組み作成（所要時間目安：30分）






## 2026-02-16 Learning Achievements

### 📚 Completed Tasks
- [x] Consider the overall design and division of roles for a Tic-Tac-Toe project
- [x] Understand the concept of encapsulation (IIFE and factory functions)
- [x] Create pseudocode for the entire project (design phase)

### 💡 Key Learning Points
**New Learnings:**
- **IIFE (Immediately Invoked Function) and Module Pattern:** I learned how to execute a function immediately upon definition and hide its internal variables from the outside world (encapsulation) using the `(function() { ... })();` syntax.
- **Singleton (Single Instance):** I learned that IIFEs are suitable for objects that should only exist once within an app, such as a game board or a referee.

Deepening Understanding:
- **Difference between Factory Functions and IIFEs:** I learned the clear rule that "Factory Functions" are used for things like players that require multiple copies, and "IIFEs" are used for things that only require one copy.
- **Building a House from the Inside Out:** I learned design thinking that starts with building the "logic (internals)" that runs on the console, rather than creating the UI (appearance) first.

**Troubleshooting:**
- **Resolving the "Why IIFE and Not a Regular Function?" Question:** I was able to clarify that an IIFE is necessary to protect data from being arbitrarily rewritten from the outside, and as a constraint to prevent accidentally creating a second one and introducing bugs.

### 🔗 Implementation Code
- Repository: https://github.com/LJDJGC/tictactoe-project-top
- Main Code (Concept):
```javascript
// Gameboard: Only one is needed, so it's an IIFE (module).
const gameboard = (function() {
let board = ["", "", "", "", "", "", "", "", ""]; // Private variable.
const getBoard = () => board;
return { getBoard };
})();

// Player: Two players are needed, so a factory function is used.
const Player = (name, marker) => {
return { name, marker };
};
```

### ⏭️ Next Action
- [ ] Implementing the `Gameboard` Object (Array Retrieval, Value Update, and Reset Functions) (Estimated Time: 45 minutes)
- [ ] `Player` Implementing a factory and instantiating it (Estimated time: 15 minutes)
- [ ] Creating the `GameController` framework (Estimated time: 30 minutes)