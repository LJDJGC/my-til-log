## 2026-03-03 学習実績

### 📚 完了タスク
- [x] The Odin Project: Tic Tac Toe - 勝利判定ロジック (`checkWinner`) の実装
- [x] 2次元配列 (`winningConditions`) を使用した盤面チェック処理の記述
- [x] `gameController` 内へのロジック統合

### 💡 学びのポイント
**新規習得：**
- **2次元配列の走査：** 配列の中に配列が入っている構造 (`[[0,1,2], ...]`) を `for` ループで処理する方法。
- **勝利条件のパターン化：** 盤面のインデックスを使って、勝ちパターンをデータとして定義する手法。

**理解深化：**
- **論理積 (`&&`) の活用：** 「AかつBかつC」という条件式を使って、3つのマークが揃っているかを判定するロジック。
- **バグ防止の重要性：** `board[a] !== ""` (空文字チェック) を入れないと、未入力のマス同士が等しいと判定されてしまう問題への対処。

**トラブルシューティング：**
- **課題：** `winningConditions` のループ処理をどう書けばよいか分からなかった。
- **解決：** `for` ループでインデックス `i` を回し、`winningConditions[i]` で個別の勝ちパターン配列を取り出し、それをさらに展開して `board` 配列のインデックスとして使う流れを理解・実装した。

### 🔗 実装コード
- リポジトリ：未作成（ローカル開発中）
- 主要コード：勝利判定ロジック
```javascript
const checkWinner = () => {
    const board = Gameboard.getBoard();
    // 勝ちパターン8通りを走査
    for (let i = 0; i < winningConditions.length; i++) {
        const [a, b, c] = winningConditions[i]; // 分割代入で見やすく
        // 1. 空ではない 2. aとbが同じ 3. aとcが同じ
        if (board[a] && board[a] === board[b] && board[a] === board[c]) {
            return true;
        }
    }
    return false;
};
```

### ⏭️ Next Action
- [ ] `playRound` 関数を完成させ、コンソール上でゲームをプレイ可能にする（所要時間目安：45分）
    - マーカー配置 → 勝利判定 → (勝負つかずなら)ターン交代 → 盤面表示 のフローをつなげる。
- [ ] 引き分け判定（盤面が埋まったが勝者がいない）を追加する（所要時間目安：15分）






## 2026-03-03 Learning Achievements

### 📚 Completed Tasks
- [x] The Odin Project: Tic Tac Toe - Implementing Victory Determination Logic (`checkWinner`)
- [x] Writing a Board State Check Process Using a 2D Array (`winningConditions`)
- [x] Integrating Logic into `gameController`

### 💡 Learning Points
**New Learnings:**
- **Traversing 2D Arrays:** How to process arrays within arrays (`[[0,1,2], ...]`) using a `for` loop.
- **Patterning Victory Conditions:** A method for defining winning patterns as data using board indexes.

**Deepening Understanding:**
- **Utilizing Logical AND (`&&`):** Logic for determining whether three marks are present using the condition "A AND B AND C".
- **Importance of bug prevention:** Addressed an issue where empty squares would be judged to be equal if `board[a] !== ""` (empty string check) was not included.

**Troubleshooting:**
- **Challenge:** I didn't know how to write the loop for `winningConditions`.
- **Solution:** I understood and implemented the process of iterating through index `i` in a `for` loop, extracting individual winning pattern arrays with `winningConditions[i]`, and then further expanding them to use as indexes for the `board` array.

### 🔗 Implementation Code
- Repository: Not yet created (currently under development locally)
- Main Code: Winning Determination Logic
``` javascript
const checkWinner = () => {
const board = Gameboard.getBoard();
// Scan for 8 winning patterns
for (let i = 0; i < winningConditions.length; i++) {
const [a, b, c] = winningConditions[i]; // Make it easier to read using destructuring assignment
// 1. Not empty 2. a and b are the same 3. a and c are the same
if (board[a] && board[a] === board[b] && board[a] === board[c]) {
return true;
}
}
return false;
};
```

### ⏭️ Next Action
- [ ] Complete the `playRound` function and make the game playable on the console (Estimated time: 45 minutes)
- Connect the flow of marker placement → victory determination → (if the game is tied) turn change → display the board.
- [ ] Add a draw determination (when the board is filled but there is no winner) (estimated time required: 15 minutes)