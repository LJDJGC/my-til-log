## 2026-02-02 学習実績

### 完了タスク
- [x] TOP JavaScript Course: 「Objects and Object Constructors」セクション完了
- [x] Project: Library - 基礎ロジックの実装（Step 1〜2相当）
- [x] オブジェクトコンストラクターによる書籍データの構造化
- [x] `crypto.randomUUID()` を用いた一意なID付与機能の実装
- [x] Gitリポジトリの初期化とGitHub連携（初期エラーの解決）

### 技術資産
- **JavaScript:** `Constructor Functions`, `new` キーワードの内部挙動, `this` によるプロパティ紐付け, メモリ効率を意識した `prototype` へのメソッド定義
- **Web API:** `crypto.randomUUID()` による衝突を避けたID生成
- **Git:** `Conventional Commits` に基づくメッセージ運用（feat, chore）, 空ブランチのプッシュエラー（refspec）の解消法
- **Software Design:** データ（State）と表示（UI）を分離する「責務の分離」の概念

### 実装コード
```
https://github.com/LJDJGC/library-project-top
```

### Next Action
- [ ] `displayBooks` 関数の実装（配列データをDOMへ反映）
- [ ] `document.createElement` を用いた動的なカード要素の生成
- [ ] CSS Flexbox/Grid を用いたライブラリのレイアウト作成




## 2026-02-02 Learning Achievements

### Completed Tasks
- [x] TOP JavaScript Course: Completed "Objects and Object Constructors" section
- [x] Project: Library - Implementing basic logic (equivalent to Steps 1-2)
- [x] Structuring book data using object constructors
- [x] Implementing a unique ID assignment function using `crypto.randomUUID()`
- [x] Initializing a Git repository and GitHub integration (resolving initial errors)

### Technical Assets
- **JavaScript:** `Constructor Functions`, the internal behavior of the `new` keyword, property binding using `this`, defining methods for `prototype` with memory efficiency in mind
- **Web API:** Collision-free ID generation using `crypto.randomUUID()`
- **Git:** Message operation based on `Conventional Commits` (feat. chore), How to resolve the empty branch push error (refspec)
- **Software Design:** The concept of "separation of responsibilities": separating data (State) and display (UI)

### Implementation code
```
https://github.com/LJDJGC/library-project-top
```

### Next Action
- [ ] Implementing the `displayBooks` function (reflecting array data in the DOM)
- [ ] Dynamically generating card elements using `document.createElement`
- [ ] Creating a library layout using CSS Flexbox/Grid





## 2026-02-02 学習実績

### 完了タスク
- [x] TOP JavaScript "Library" プロジェクトの初期実装開始
- [x] `Book` コンストラクタによる書籍データ構造の定義
- [x] `crypto.randomUUID()` を活用した一意のデータ識別子の付与
- [x] `forEach` を用いた配列データからDOM要素（カード）への変換ロジック構築

### 技術資産
- **JavaScript (DOM)**: `document.querySelector`, `createElement`, `appendChild` を組み合わせた動的なUI構築手法
- **実行環境の差異**: Node.js（サーバーサイド）とブラウザ環境（クライアントサイド）におけるグローバルオブジェクト（`document` 等）の有無の理解
- **関数実行の厳密性**: 関数名のみ（参照）と `()` を伴う呼び出し（実行）の挙動の違い
- **デバッグスキル**: Node.js のエラーログから環境の不一致を特定するプロセス

### 実装コード
```
未作成（ローカル環境にて script.js / index.html 実装中）
```

### Next Action
- [ ] `index.html` への `<script>` タグ追加による JS 読み込みの有効化
- [ ] ブラウザ上での `displayBook()` 実行と書籍データの初描画確認
- [ ] 再描画時の重複を防ぐための「コンテナ初期化（`innerHTML = ""`）」の実装
- [ ] 1冊のカード内にタイトルだけでなく著者・ページ数等を表示する機能拡張





## 2026-02-02 Learning Achievements

### Completed Tasks
- [x] Beginning initial implementation of the TOP JavaScript "Library" project
- [x] Defining a book data structure using the `Book` constructor
- [x] Assigning unique data identifiers using `crypto.randomUUID()`
- [x] Building logic to convert array data to DOM elements (cards) using `forEach`

### Technical Assets
- **JavaScript (DOM)**: A dynamic UI construction method combining `document.querySelector`, `createElement`, and `appendChild`
- **Differences in Execution Environments**: Understanding the presence or absence of global objects (such as `document`) in Node.js (server-side) and browser environments (client-side)
- **Strictness of Function Execution**: Differences in behavior between function name alone (reference) and invocation with `()` (execution)
- **Debugging Skills**: Node.js Process for identifying environment inconsistencies from the error log

### Implementation Code
```
Not yet created (Implementing script.js / index.html in a local environment)
```

### Next Action
- [ ] Enable JS loading by adding a `<script>` tag to `index.html`
- [ ] Run `displayBook()` in the browser and confirm initial rendering of book data
- [ ] Implement "container initialization (`innerHTML = ""`)" to prevent overlaps during redrawing
- [ ] Enhanced functionality to display not only the title but also the author, page count, etc. within a single book card