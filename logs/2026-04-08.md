## 2026-04-08 学習実績

### 📚 完了タスク
- [x] Vite開発サーバーのセキュリティ（allowedHosts）エラーの解決
- [x] React公式チュートリアル：`map` メソッドを用いた着手履歴リストのレンダリング

### 💡 学びのポイント
**新規習得：**
- **Viteのセキュリティ仕様（DNSリバインディング対策）**：外部ホスト（CodeSandboxのURL）からのプレビューアクセスを許可するために、`vite.config.ts` の `server.allowedHosts` を設定する必要があるという、フロントエンドのインフラ設定の基礎を習得した。
- **配列からUIへの変換（`map`）**：JavaScriptの `Array.prototype.map()` を使用して、`history`（過去の手番の配列）のデータを Reactの `<button>` 要素（UI）の配列に変換し、画面にリスト表示する手法を実装した。

**理解深化：**
- **動的リストにおける問題の発見**：`map`でリストをレンダリングした結果、コンソールに `Warning: Each child in a list should have a unique "key" prop.` という警告が出た。ReactがDOMの変更を効率的に検知するために、一意の識別子（`key`）が必要であることを体感として理解した（解決は次回）。

**トラブルシューティング：**
- **課題**：CodeSandbox上でViteのプレビュー画面が「Blocked request」となり表示されない。
- **解決方法**：エラーメッセージの指示に従い、`vite.config.ts` にアクセス元のURLを許可する設定（`server.allowedHosts`）を追記することで解決。エラー文を読み解き、構成ファイルを書き換える対応力が身についた。

### 🔗 実装コード
- リポジトリ：未作成（CodeSandbox上のローカルGit）
- 主要コード：
```javascript
// history配列をmapでループし、過去の着手へ戻るボタンのリストを生成する
const moves = history.map((squares, move) => {
  let description;
  if (move > 0) {
    description = 'Go to move #' + move;
  } else {
    description = 'Go to game start';
  }
  return (
    // ※現在はkeyプロパティがないためコンソールエラーが発生している状態
    <li>
      <button onClick={() => jumpTo(move)}>{description}</button>
    </li>
  );
});
```

### ⏭️ Next Action
- [ ] React公式チュートリアル：リストの `key` エラーの解消と、`jumpTo` 関数の実装（タイムトラベルの完成）（所要時間目安：60分）







## Learning Record - April 8, 2026

### 📚 Completed Tasks
- [x] Resolved a security (allowedHosts) error on the Vite development server
- [x] React Official Tutorial: Rendering a Start History List Using the `map` Method

### 💡 Learning Points
**Newly Acquired:**
- **Vite Security Specification (DNS Rebinding Countermeasures)**: Learned the basics of frontend infrastructure configuration, including the need to set `server.allowedHosts` in `vite.config.ts` to allow preview access from external hosts (CodeSandbox URLs).
- **Converting Arrays to UI (`map`)**: Implemented a method to convert `history` (an array of past moves) data into an array of React `<button>` elements (UI) using JavaScript's `Array.prototype.map()` and display it as a list on the screen.

**Deepening Understanding:**
- **Discovering a Problem with Dynamic Lists:** When rendering a list with `map`, the console displayed the warning: `Warning: Each child in a list should have a unique "key" prop.`. I intuitively understood that React requires a unique identifier (`key`) to efficiently detect DOM changes (solution to follow).

**Troubleshooting:**
- **Issue:** The Vite preview screen on CodeSandbox displayed "Blocked request" and did not appear.
- **Solution:** Following the error message's instructions, I added a setting to `vite.config.ts` to allow the source URL (`server.allowedHosts`), which solved the problem. I developed the ability to interpret error messages and modify configuration files.

### 🔗 Implementation Code
- Repository: Not yet created (Local Git on CodeSandbox)
- Main Code:
```javascript
// Loop through the history array with map and generate a list of buttons to return to previous moves
const moves = history.map((squares, move) => {
let description;
if (move > 0) {
description = 'Go to move #' + move;
} else {
description = 'Go to game start';
}
return (
// *Currently a console error occurs because the key property does not exist
<li>
<button onClick={() => jumpTo(move)}>{description}</button>
</li>
);
});
```
### ⏭️ Next Action
- [ ] React Official Tutorial: Resolving the `key` error in lists and implementing the `jumpTo` function (Completing Time Travel) (Estimated time: 60 minutes)











## 2026-04-08 学習実績（昼活）

### 📚 完了タスク
-[x] React公式チュートリアル（Tic Tac Toe）：タイムトラベル機能における、過去の手番からの履歴上書きロジック（`handlePlay`）の実装

### 💡 学びのポイント
**新規習得：**
- **`slice()` と スプレッド構文 `...` の高度な組み合わせ**：`[...history.slice(0, currentMove + 1), nextSquares]` と記述することで、「配列の特定の時点までを切り出し、そこに新しい要素をガッチャンコする」という実務で頻出する配列操作パターンを習得した。

**理解深化：**
- **「やり直し」のアルゴリズム実装**：過去の手番に戻って新しくマスをクリックした際、未来の履歴を残したまま追加するのではなく、`currentMove`（現在の手番）を基準に「それ以降の履歴を切り捨てる（上書きする）」という、タイムトラベルのコアとなるロジックを理解できた。
- **学習戦略の最適化**：活字主体の公式ドキュメントと動画教材を併用することで、動画で得た「わかりやすい概念・講師の実務的な視点」が公式の難解な文章を読み解く鍵になることを実感した。

**トラブルシューティング：**
- 特になし（ロジックの変更をスムーズに実装完了）

### 🔗 実装コード
- リポジトリ：未作成（ローカル環境）
- 主要コード：
```javascript
// Gameコンポーネント内の着手処理
function handlePlay(nextSquares) {
  // 現在の手番(currentMove)までの履歴を切り出し、そこに最新の盤面(nextSquares)を追加する
  const nextHistory =[...history.slice(0, currentMove + 1), nextSquares];
  setHistory(nextHistory);
  // 現在の手番を、追加した最新の履歴のインデックスに更新する
  setCurrentMove(nextHistory.length - 1);
  // xIsNextのトグル処理は削除（currentMoveから計算できるようにするため。※チュートリアルの進行による）
}
```

### ⏭️ Next Action
- [ ] React公式チュートリアル：三目並べの最終動作確認とリファクタリングを完了させる（所要時間目安：30分）
- [ ] YouTube動画（セイト先生等）を用いた「React + TypeScript」基礎チュートリアルへの本格移行準備（所要時間目安：30分）








## 2026-04-08 Learning Record (Daytime Activities)

### 📚 Completed Tasks
-[x] React Official Tutorial (Tic Tac Toe): Implementing the history overwrite logic (`handlePlay`) from past moves in the time travel function

### 💡 Learning Points
**Newly Acquired:**
- **Advanced combination of `slice()` and spread syntax `...`**: By writing `[...history.slice(0, currentMove + 1), nextSquares]`, I learned a frequently used array manipulation pattern in practice: "extracting an array up to a specific point in time and adding a new element to it."

**Deepening Understanding:**
- **Implementation of the "Redo" Algorithm:** I understood the core logic of time travel: when returning to a past turn and clicking a new square, instead of adding it while retaining the future history, it "truncates (overwrites) the history from `currentMove` (the current turn)" as the basis.
- **Optimization of Learning Strategy:** By using both the text-based official documentation and video materials, I realized that the "easy-to-understand concepts and practical perspectives from the instructor" gained from the videos were key to deciphering the complex text of the official documentation.

**Troubleshooting:**
- None in particular (Logic changes were implemented smoothly)

### 🔗 Implementation Code
- Repository: Not created (Local environment)
- Main Code:
```javascript
// Game component's move processing
function handlePlay(nextSquares) {
// Extract the history up to the current turn (currentMove) and add the latest board state (nextSquares) to it
const nextHistory =[...history.slice(0, currentMove + 1), nextSquares];
setHistory(nextHistory);
// Update the current turn to the index of the newly added history
setCurrentMove(nextHistory.length - 1);
// Remove the xIsNext toggle (to allow calculation from currentMove. *Depending on the tutorial progress)
}
```

### ⏭️ Next Action
- [ ] React Official Tutorial: Completing Final Functionality Checks and Refactoring for Tic-Tac-Toe (Estimated Time: 30 minutes)
- [ ] Preparing for a Full Transition to the "React + TypeScript" Basic Tutorial Using YouTube Videos (e.g., by Seito-sensei) (Estimated Time: 30 minutes)







## 2026-04-08 学習実績（夜活）

### 📚 完了タスク
- [x] React公式チュートリアル：不要なState（`xIsNext`）の削除と、`currentMove` からの派生状態（Derived State）へのリファクタリング
- [x] 動画教材（セイト先生 React講座 #9）：Reactのイベント処理、SyntheticEvent（合成イベント）、および関数定義のベストプラクティスの学習

### 💡 学びのポイント
**新規習得：**
- **Derived State（派生状態）**：`xIsNext` のような「他のState（`currentMove`）から計算可能な値」は `useState` で管理せず、レンダリング時に計算して求めることで、状態の不整合（バグ）を根絶する設計思想を習得した。
- **SyntheticEvent（合成イベント）**：Reactの `e`（イベントオブジェクト）は、ブラウザのネイティブイベントをラップし、どのブラウザでも同じように動作するよう吸収（クロスブラウザ対応）してくれているラッパーであることを知った。
- **関数定義とパフォーマンス**：コンポーネントの内部に関数を定義すると、再レンダリングのたびに関数が「再生成」されパフォーマンスに影響が出る可能性があること。そしてそれを防ぐための `useCallback` というフックの存在（概念）を学んだ。

**理解深化：**
- **Single Source of Truth（信頼できる単一の情報源）**：履歴（`history`）と現在手番（`currentMove`）だけを「真実（State）」とし、残りはすべてそこから計算（UIの描画や手番の判定）するという、Reactのデータフローの真髄を体感した。
- **コードの可読性**：`onClick` の中に無名関数で長々と処理を書くのは見通しが悪くなるため、コンポーネントの内外に名前付き関数（ハンドラー）として切り出すという実務的なコーディング規約（Lintの考え方）への理解が深まった。

**トラブルシューティング：**
- 特になし（リファクタリングにより、潜在的な「State同期ズレ」のバグを未然に防ぐことに成功）

### 🔗 実装コード
- リポジトリ：未作成（ローカル環境）
- 主要コード：
```javascript
// Boardコンポーネント（またはGameコンポーネント）内

// ❌ 以前のコード（Stateが2つあり、同期漏れのリスクがあった）
// const[xIsNext, setXIsNext] = useState(true);
// const [currentMove, setCurrentMove] = useState(0);

// ⭕️ 修正後のコード（Stateを1つにし、xIsNextは計算で求める）
const [currentMove, setCurrentMove] = useState(0);

// currentMoveが偶数ならXの手番、奇数ならOの手番（Derived State）
const xIsNext = currentMove % 2 === 0;

function handlePlay(nextSquares) {
  // ...履歴の更新処理...
  // setXIsNext(!xIsNext); // 👈 この不要な更新処理を削除でき、コードが堅牢になった
}
```

### ⏭️ Next Action
-[ ] React公式チュートリアル：三目並べの最終機能（`jumpTo`によるタイムトラベル完成）と動作確認（所要時間目安：30分）
- [ ] 次のステップ：TypeScriptの基礎インプット開始（所要時間目安：60分）





## 2026-04-08 Learning Results (Evening Session)

### 📚 Completed Tasks
- [x] React Official Tutorial: Removing Unnecessary State (`xIsNext`) and Refactoring to Derived State from `currentMove`
- [x] Video Material (Seito-sensei React Course #9): Learning about React Event Handling, SyntheticEvent (Synthetic Event), and Best Practices for Function Definition

### 💡 Key Learning Points
**Newly Acquired:**
- **Derived State**: Learned the design philosophy of eliminating state inconsistencies (bugs) by calculating values ​​that can be calculated from other States (`currentMove`), such as `xIsNext`, at rendering time, rather than managing them with `useState`.

- **Synthetic Events**: I learned that React's `e` (event object) is a wrapper that wraps the browser's native events, absorbing them so that they behave the same way across browsers (cross-browser compatibility).

- **Function Definition and Performance**: I learned that defining functions inside a component can affect performance because the function is "regenerated" every time the component is re-rendered. I also learned about the existence (concept) of the `useCallback` hook to prevent this.

**Deeper Understanding:**

- **Single Source of Truth**: I experienced the essence of React's data flow, where only history (`history`) and current move (`currentMove`) are considered "true (State)," and everything else is calculated from them (UI rendering and turn determination). - **Code Readability**: Writing lengthy code in anonymous functions within `onClick` makes the code difficult to read. This deepened my understanding of the practical coding convention (the Lint approach) of extracting this code into named functions (handlers) both inside and outside the component.

**Troubleshooting:**
- None in particular (Refactoring successfully prevented a potential "State synchronization mismatch" bug)

### 🔗 Implementation Code
- Repository: Not created (Local environment)
- Main code:
```javascript
// Inside the Board component (or Game component)

// ❌ Previous code (Two States, risk of synchronization failure)
// const[xIsNext, setXIsNext] = useState(true);
// const [currentMove, setCurrentMove] = useState(0);

// ⭕️ Corrected code (One State, xIsNext calculated)
const [currentMove, setCurrentMove] = useState(0);

// If currentMove is even, it's X's turn; if odd, it's O's turn (Derived State)
const xIsNext = currentMove % 2 === 0;

function handlePlay(nextSquares) {
// ...history update process...
// setXIsNext(!xIsNext); // 👈 This unnecessary update process can be removed, making the code more robust.
}
```

### ⏭️ Next Action
-[ ] React Official Tutorial: Final functionality of Tic-Tac-Toe (Time travel completed with `jumpTo`) and operation check (Estimated time: 30 minutes)
- [ ] Next step: Start learning the basics of TypeScript (Estimated time: 60 minutes)