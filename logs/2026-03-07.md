## 2026-03-07 学習実績

### 📚 完了タスク
- [x] The Odin Project: JavaScript Course - Project: Tic Tac Toe
- [x] `ScreenController` の構文エラー・タイポの修正
- [x] メッセージ表示用DOM要素（`.message`）の分離と修正
- [x] 現在のアクティブプレイヤーを画面のテキストに連動させるUI実装

### 💡 学びのポイント
**新規習得：**
- **DOM要素のリセット時の挙動**：親要素に対して `textContent = ""` を実行すると、その中にあった子要素（HTMLタグ）もすべて消滅してしまうことを学んだ。UIの枠組み（コンテナ）は独立させて配置する設計が重要。

**理解深化：**
- **変数の「型」の意識**：`activePlayer` が「関数を呼び出した結果のオブジェクト」であるにも関わらず、再度関数として `()` を付けて呼び出してしまった。エラーログを読んで原因を特定し、型を意識してプロパティ（`.name`）にアクセスする重要性を実感した。

**トラブルシューティング：**
- `TypeError: activePlayer is not a function` エラーに対し、余計な `()` を削除することで修正。
- 画面が描画されないバグに対し、スペルミスや `gameController` からの公開（エクスポート）漏れを特定して修正した。

### 🔗 実装コード
- リポジトリ：未作成
- 主要コード：
```javascript
// ScreenController 内の画面更新ロジック
const updateScreen = () => {
    const board = Gameboard.getBoard();
    const activePlayer = gameController.getActivePlayer(); // オブジェクトを取得

    boardDiv.textContent = ""; // 盤面のみをリセット
    // オブジェクトのプロパティに正しくアクセスして画面に表示
    boardMessage.textContent = `${activePlayer.name} , It's your turn.`;

    // ...マス目の生成処理...
};
```

### ⏭️ Next Action
- [ ] `gameController` 内に勝敗メッセージを保存する変数（例: `gameMessage`）を作成し、`checkWinner` 時等に代入する（所要時間目安：15分）
- [ ] `gameController` から `getGameMessage` を return し、`ScreenController` で勝敗に応じたメッセージを出し分ける（所要時間目安：20分）
- [ ] HTMLに `<button class="restart">Restart</button>` を追加し、盤面と状態を初期化する処理を実装する（所要時間目安：30分）







## 2026-03-07 Learning Achievements

### 📚 Completed Tasks
- [x] The Odin Project: JavaScript Course - Project: Tic Tac Toe
- [x] Fixing syntax errors and typos in `ScreenController`
- [x] Separating and correcting the DOM element for displaying messages (`.message`)
- [x] Implementing a UI that links the currently active player to on-screen text

### 💡 Key Points
**New Learnings:**
- **DOM Element Behavior When Resetting**: I learned that when executing `textContent = ""` on a parent element, all of its child elements (HTML tags) disappear. It is important to design UI frameworks (containers) so that they are placed independently.

**Deepened Understanding:**
- **Variable Type Awareness**: Even though `activePlayer` is the "object resulting from a function call," I called it again as a function using `()`. I identified the cause by reading the error log and realized the importance of accessing properties (`.name`) with type awareness.

**Troubleshooting:**
- Fixed the `TypeError: activePlayer is not a function` error by removing an unnecessary `()`.
- Fixed a bug that caused the screen not to draw, caused by a spelling error and missing exports from `gameController`.

### 🔗 Implementation Code
- Repository: Not yet created
- Main Code:
```javascript
// Screen update logic within ScreenController
const updateScreen = () => {
const board = Gameboard.getBoard();
const activePlayer = gameController.getActivePlayer(); // Get the object

boardDiv.textContent = ""; // Reset only the board
/ Correctly access the object's properties and display them on the screen
boardMessage.textContent = `${activePlayer.name} , It's your turn.`;

/ ...Grid generation process...
};
```

### ⏭️ Next Action
- [ ] Create a variable (e.g., `gameMessage`) within `gameController` to store the victory or defeat message, and assign it during `checkWinner`, etc. (Estimated time: 15 minutes)
- [ ] Return `getGameMessage` from `gameController` Use `ScreenController` to display different messages depending on the outcome (Estimated time: 20 minutes)
- [ ] Add `<button class="restart">Restart</button>` to the HTML and implement a process to reset the board and state (Estimated time: 30 minutes)