## 2026-07-17 学習実績

### 📚 完了タスク
- [x] YouTube API キーの制限設定トラブルシューティング
  - エラー：`Requests from referer <empty> are blocked.`
  - 原因：HTTPリファラー制限がサーバー間通信をブロック
  - 解決：制限を「なし」に変更、API制限でYouTube Data API v3のみに絞る
- [x] YouTube 検索の動作確認（`npm run dev` + キーワード検索が成功）
- [x] セキュリティ設計の評価と議論
  - プロキシパターンでAPIキーはサーバーサイドのみ
  - API制限で悪用範囲を限定
- [x] STEP 2：`src/app/api/youtube/search/route.ts` の1行ずつ解説
  - Route Handler の仕組み（`export async function GET`）
  - 型定義（`YouTubeSearchItem`, `YouTubeVideoResult`）
  - クエリパラメータの取得とバリデーション
  - YouTube API への `fetch()` + `await`
  - レスポンスの整形（`.map()` + `?.` + `||`）
  - `try/catch` によるエラーハンドリング

### 💡 学びのポイント
**新規習得：**
- **APIキーの「アプリケーション制限」とサーバー間通信の関係**:
  Route Handler (サーバー) → YouTube API (外部サーバー) の通信には Referer ヘッダーが付かない。
  そのため「HTTPリファラー制限」を設定すると、サーバーサイドからのリクエストはすべてブロックされる。
  解決策：API制限で使用できるAPIを限定することで、キーが漏れても被害を最小限にする。
- **Route Handler の `export async function GET`**:
  Next.js App Router では、`src/app/api/xxx/route.ts` に `export async function GET()` と書くだけで
  `GET /api/xxx` のエンドポイントになる。関数名がHTTPメソッドに対応。
- **`response.ok` の使い方**:
  `fetch()` のレスポンスには `ok` プロパティがあり、HTTPステータスが200番台なら `true`、それ以外なら `false`。
  エラーハンドリングで便利。`if (!response.ok)` で「失敗した場合」を表現できる。

**理解深化：**
- **Route Handler は「中継役（プロキシ）」**:
  ブラウザ → 自サーバー → YouTube のように、間に入ることでAPIキーを隠せる。
  ブラウザから見えるのは自サーバーのURLだけ。キーは最後までブラウザに渡らない。
- **`?.`（オプショナルチェイニング）と `||` の使い分け**:
  ```tsx
  // ?. → プロパティが存在したらアクセス、なければ undefined
  // || → 左が falsy（undefined, null, 空文字, 0）なら右を使う
  item.snippet.thumbnails?.medium?.url || item.snippet.thumbnails?.default?.url || ""
  ```
  `?.` で安全に深いプロパティにアクセスし、`||` でフォールバック値を設定する。
- **`.map()` で「変換」する**:
  APIの生レスポンスをアプリ用のシンプルな形に変換する。元のデータを変更するのではなく、
  必要な項目だけを抜き出した「新しい配列」を作る。

### 🔗 実装コード
- リポジトリ：`routine-tracker`（`draft/youtube-integration` ブランチ）
- 解説したファイル：
  - `src/app/api/youtube/search/route.ts`（YouTube API プロキシ、全97行）
- 主要コード：
```tsx
export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url);
  const query = searchParams.get("q");
  if (!query || query.trim().length === 0) {
    return NextResponse.json({ error: "required" }, { status: 400 });
  }
  const apiKey = process.env.YOUTUBE_API_KEY;
  const url = new URL("https://www.googleapis.com/youtube/v3/search");
  url.searchParams.set("part", "snippet");
  url.searchParams.set("q", query);
  url.searchParams.set("key", apiKey);
  url.searchParams.set("maxResults", "10");
  url.searchParams.set("type", "video");
  const response = await fetch(url.toString());
  const data = await response.json();
  const results = (data.items || []).map((item) => ({
    videoId: item.id.videoId,
    title: item.snippet.title,
    channelTitle: item.snippet.channelTitle,
    thumbnailUrl: item.snippet.thumbnails?.medium?.url || "",
  }));
  return NextResponse.json({ results });
}
```

### ⏭️ Next Action
- [ ] STEP 2 続き：`useYouTubePlayer.ts` フックの解説（所要時間目安：45分）
- [ ] STEP 3：写経（feat/handson-youtube ブランチ作成）（所要時間目安：60分）


